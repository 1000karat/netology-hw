# Домашнее задание к занятию [Работа с данными (DDL/DML)](https://github.com/netology-code/sdb-homeworks/blob/main/12-02.md)
*Автор: Алексей Сысоев*

## 📌 Оглавление
[Задание 1](#Задание-1)  
[Задание 2](#Задание-2)  
[Задание 3](#Задание-3)
##

### Задание 1 
1.1. Поднимите чистый инстанс MySQL версии 8.0+. Можно использовать локальный сервер или контейнер Docker.

1.2. Создайте учётную запись sys_temp.

1.3. Выполните запрос на получение списка пользователей в базе данных. (скриншот)

1.4. Дайте все права для пользователя sys_temp.

1.5. Выполните запрос на получение списка прав для пользователя sys_temp. (скриншот)

1.6. Переподключитесь к базе данных от имени sys_temp.

Для смены типа аутентификации с sha2 используйте запрос:
```sql
ALTER USER 'sys_test'@'localhost' IDENTIFIED WITH mysql_native_password BY 'password';
```
1.6. По ссылке https://downloads.mysql.com/docs/sakila-db.zip скачайте дамп базы данных.

1.7. Восстановите дамп в базу данных.

1.8. При работе в IDE сформируйте ER-диаграмму получившейся базы данных. При работе в командной строке используйте команду для получения всех таблиц базы данных. (скриншот)

*Результатом работы должны быть скриншоты обозначенных заданий, а также простыня со всеми запросами.*

#### 🔧 Решение
Для решения использовался [docker compose](https://github.com/1000karat/netology-hw/blob/main/files/hw-013/docker-compose.yml) с версией mysql 8.0.46.
1. 
```shell script
$ docker compose up -d
$ docker exec -it mysql8046 mysql -uroot -prootpass
```
2. 
```sql
CREATE USER 'sys_temp'@'localhost' IDENTIFIED BY 'password';
```
3. 
```sql
SELECT user, host FROM mysql.user;
```
📌 Скриншот:
![Скриншот](https://raw.githubusercontent.com/1000karat/netology-hw/refs/heads/main/img/hw-013-01.png)
4. 
```sql
GRANT ALL PRIVILEGES ON *.* TO 'sys_temp'@'localhost' WITH GRANT OPTION;
FLUSH PRIVILEGES;
```
📌 Скриншот:
![Скриншот](https://raw.githubusercontent.com/1000karat/netology-hw/refs/heads/main/img/hw-013-02.png)
5. 
```shell script
docker exec -it mysql8046 mysql -usys_temp -ppassword
```
7. Скопировать в контейнер:
```shell script
docker cp sakila-schema.sql mysql8046:/sakila-schema.sql
docker cp sakila-data.sql mysql8046:/sakila-data.sql
docker exec -it mysql8046 mysql -usys_temp -ppassword
```
Создать БД:
```sql
CREATE DATABASE sakila;
```
[Импорт](https://dev.mysql.com/doc/sakila/en/sakila-installation.html):
```shell script
docker exec -i mysql8046 mysql -usys_temp -ppassword sakila < sakila-schema.sql
docker exec -i mysql8046 mysql -usys_temp -ppassword sakila < sakila-data.sql
```
8. Для скриншота:
```sql
USE sakila;
SHOW TABLES;
```
📌 Скриншот:
![Скриншот](https://raw.githubusercontent.com/1000karat/netology-hw/refs/heads/main/img/hw-013-03.png)

### Задание 2 
Составьте таблицу, используя любой текстовый редактор или Excel, в которой должно быть два столбца: в первом должны быть названия таблиц восстановленной базы, во втором названия первичных ключей этих таблиц. Пример: (скриншот/текст)
```
Название таблицы | Название первичного ключа
customer         | customer_id
```

#### 🔧 Решение
| Название таблицы | Первичный ключ (Primary Key) |
| ---------------- |------------------------------|
| actor | `actor_id`                   |
| address | `address_id`                 |
| category | `category_id`                |
| city | `city_id`                    |
| country | `country_id`                 |
| customer | `customer_id`                |
| film | `film_id`                    |
| film_actor | `actor_id`, `film_id`        |
| film_category | `film_id`, `category_id`     |
| film_text | `film_id`                    |
| inventory | `inventory_id`               |
| language | `language_id`                |
| payment | `payment_id`                 |
| rental | `rental_id`                  |
| staff | `staff_id`                   |
| store | `store_id`                   |


### Задание 3
3.1. Уберите у пользователя sys_temp права на внесение, изменение и удаление данных из базы sakila.

3.2. Выполните запрос на получение списка прав для пользователя sys_temp. (скриншот)

*Результатом работы должны быть скриншоты обозначенных заданий, а также простыня со всеми запросами.*

#### 🔧 Решение
1. Т.к. в первом задании выдавали глобальные права, необходимо их забрать и добавить на `sakila`
```sql
REVOKE ALL PRIVILEGES ON *.* FROM 'sys_temp'@'localhost';
REVOKE GRANT OPTION ON *.* FROM 'sys_temp'@'localhost';
FLUSH PRIVILEGES;
GRANT SELECT ON sakila.* TO 'sys_temp'@'localhost';
```
2. 
```sql
SHOW GRANTS FOR 'sys_temp'@'localhost';
```
📌 Скриншот:
![Скриншот](https://raw.githubusercontent.com/1000karat/netology-hw/refs/heads/main/img/hw-013-04.png)
